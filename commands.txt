* command for chk git version
  >git version

*cmd for listing config detail
 >git config --list

* cmd for adding user email
 >git config --global user.email "email@gmail.com"

*git cmd for initialising git repository
 >git init

*cmd for adding all untraced files to staging area
>git add -A

*cmd for adding staged files to local repository
 > git commit -m "give msg"

 *cmd for pushing changes from local repo to remote repo
 > git push

 *cmd for listing all untracked files
 >git status